{
  "private": true,
  "devDependencies": {
    "lerna": "^3.15.0"
  },
  "scripts": {
    "link": "lerna link",
    "develop": "lerna run develop --stream",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream"
  }
}
